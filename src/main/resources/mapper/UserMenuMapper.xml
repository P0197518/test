<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.sgu.www.easyui.mapper.UserMenuMapper">
    <resultMap id="baseMap" type="cn.edu.sgu.www.easyui.entity.UserMenu">
        <result column="id" property="id" />
        <result column="display" property="display" />
        <result column="user_id" property="userId" />
        <result column="menu_id" property="menuId" />
    </resultMap>

    <update id="updateByParentId">
        update user_menu set pxh = #{pxh}
        where user_id = #{userId} and menu_id in (
            select id from menu where parent_id = #{menuId}
        )
    </update>
</mapper>