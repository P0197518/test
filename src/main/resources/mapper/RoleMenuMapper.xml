<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.sgu.www.easyui.mapper.RoleMenuMapper">
    <resultMap id="baseMap" type="cn.edu.sgu.www.easyui.entity.RoleMenu">
        <result column="id" property="id" />
        <result column="role_id" property="roleId" />
        <result column="menu_id" property="menuId" />
    </resultMap>

    <insert id="insertBatch">
        insert into role_menu (id, role_id, menu_id) values
        <foreach collection="menuIds" item="menuId" separator=",">
            (null, #{roleId}, #{menuId})
        </foreach>
    </insert>

    <select id="selectByCondition" resultMap="baseMap">
        select id, menu_id, role_id from role_menu
        where 1 = 1
        <if test="roleId != null">
            and role_id = #{roleId}
        </if>

        <if test="menuId != null and menuId != ''">
            and menu_id = #{menuId}
        </if>
    </select>
</mapper>